CREATE TABLE departments (
    dept_id SERIAL PRIMARY KEY,
    dept_name VARCHAR(100)
);


CREATE TABLE employees (
    emp_id SERIAL PRIMARY KEY,
    emp_name VARCHAR(100),
    dept_id INTEGER DEFAULT 0 REFERENCES departments(dept_id) ON DELETE SET DEFAULT ON UPDATE SET DEFAULT
);
-- Insert into departments
INSERT INTO departments (dept_name) VALUES ('HR');
-- Insert into employees
INSERT INTO employees (emp_name, dept_id) VALUES ('Alice', 1);
-- Delete department row
DELETE FROM departments WHERE dept_id = 1;
-- The 'dept_id' in the employees table will be set to its default value (0)
